package cl.uchile.dcc
package rank

/** Rank Eight: order 8, chip value 8, classification Even. */
object Eight extends Rank {
  val order: Int = 8
  val chipValue: Int = 8
  val classification: RankClassification = Even
}
