package cl.uchile.dcc
package rank

/** Rank Three: order 3, chip value 3, classification Odd. */
object Three extends Rank {
  val order: Int = 3
  val chipValue: Int = 3
  val classification: RankClassification = Odd
}
