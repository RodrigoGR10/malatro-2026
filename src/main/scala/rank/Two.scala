package cl.uchile.dcc
package rank

/** Rank Two: order 2, chip value 2, classification Even. */
object Two extends Rank {
  val order: Int = 2
  val chipValue: Int = 2
  val classification: RankClassification = Even
}
